# poly
Polymarket weather market analyzer. Fetches live markets, pulls real forecasts from Open-Meteo (NOAA GFS + ECMWF), and calculates edge on every outcome using ensemble probability math. Shows exact payout at your stake. No backend, no API keys, runs in the browser.
